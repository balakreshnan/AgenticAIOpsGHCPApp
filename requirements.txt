streamlit>=1.40
azure-ai-projects>=2.1.0
azure-identity>=1.19
agent-framework>=1.0.0
agent-framework-foundry>=1.0.0
azure-ai-evaluation[redteam]>=1.17
assert-ai>=0.1.0
agent-governance-toolkit[full]==4.0.0
python-dotenv>=1.0

# Microsoft RAMPART (safety-probe framework, RAMPART tab) is installed
# separately because it pins a conflicting PyRIT version. See
# requirements-rampart.txt — install it with:  pip install --no-deps -r requirements-rampart.txt
